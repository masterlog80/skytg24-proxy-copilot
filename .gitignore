node_modules/
npm-debug.log*
package-lock.json

# Runtime temp files (created by the app at runtime)
/tmp/

# Never commit actual VPN credentials/configs
# (the config/vpn/ directory is intentionally tracked for README.md only)
config/vpn/*.ovpn
